# 直線ライントレース高速化：やったことと結論

## 最新：最終青は距離窓内の実検知で決める

18-00-50/18-02-07ではコーナー後最初の青が5/4連続で、旧7連続に届かず未計数。
受付は有効。最後の青は17/18連続読めたが本数がずれて通過した。
本数方式をコーナー後だけ廃止し、ボトル色による目的地の距離予測を使う。
青1本目とエッジ持ち替え・コーナーまでは従来通り。アーム等は変更しない。

| 色 | 予測最終青 | 減速開始 | 探索窓開始 | 上限 |
|---|---:|---:|---:|---:|
| 黄 | 450 | 200 | 330 | 650 |
| 青 | 735 | 485 | 615 | 935 |
| 赤 | 1005 | 755 | 885 | 1205 |

単位mm、コーナー完了から。97/Kp0.30で移動し予測250mm手前で65/Kp0.38へ。
予測120mm手前から、非青3連続の確認後、青5連続で配置する。
ゲート開始時から青なら、その青が途切れるまで配置しない。
探索前の青や途中の青の読み落としは最終判定に影響しない。
上限外では青が成立しても配置せず停止。中央ボタンも従来通り。
120mmは今回の暫定値。距離ずれが大きければ別の青を拾う可能性は残り、
位置だけで正しい線だと保証する方式ではない。

高速移動中も同じ色読みをBlueRun/BlueGateとして記録。
BluePositionにコーナー相対開始/終了距離と車輪速度の開始/終了値[deg/s]を追加。
AreaBlueには計画探索位置・実受付位置・最初の候補位置・最終検知位置を出す。
raw連続区間は1本の青でも途切れれば複数になり、物理本数ではない。
最終青は5連続で停止するので、その後の長さは測れない。
97→65の減速前後の速度と青位置から250mmの余裕を評価する。
観測と制御のカウンタは別。走行中の診断出力は増やさず、観測は停止後に出す。

AreaBlueGateのホストテストで、3色・探索前の青・開始時青・未減速・
連続途切れ・最終青成立・上限外の青/未検知を確認。実機は未確認。

---

## 最新：行き90度コーナー後もCへ（2026-09-20）

17-45-55 / 17-46-58の2本を確認。ユーザー目視ではアーム姿勢はほぼ狙いどおり、
配置後の動作も許容。青1接続は95/97mm、649/638msで成立。これらの設定は今回維持する。

行きコーナー後は従来PWM90/Kp0.38だったため、PWM97/Kp0.30/Ki0.01/Kd0.02/減速2.3へ変更。
この区間はコーナー完了時点から高速設定とし、新たな低速接続は追加しない。
最終エリア青の予測検知位置（コーナー完了から）を黄450/青735/赤1005mmとし、
250mm手前の黄200/青485/赤755mmでPWM65/Kp0.38へ戻す。
黄/青は従来実測、赤は今回の最終青到達約1007〜1008mmに基づく暫定値。
物理的な青先端ではなく連続一致が成立する位置の予測なので、厳密な250mmではない。
従来の赤減速814mm（今回実際814/816mm）では最終青まで約190mmしか残らなかった。
途中の青2・3本目は97のまま跨ぐ。青条件7連続/最終5連続・通過40連続は変更しない。

今回の青2本目は8/9連続で確定7回に近いので、高速化による取りこぼしを重点確認する。
BlueRunごとにBlueGateとして、観測開始時の次番号・新規青検知受付状態・必要連続数・
基準PWM・平均車輪速度[deg/s]・直前の非青連続数を追加。追加の色読みは行わない。
生の青連続が足りないのか、前の青の通過待ちが終わらず数えられないのかを分ける。
最終青で走行が終了するため、その生の青連続は途中で打ち切られる点に注意する。
受付状態は連続区間開始時点のスナップショットで、途中変化までは表さない。

AreaBlueに予測位置・予定/実際の減速位置・最終検知位置・減速から検知までの距離/時間を追加。
DTraceのcorner-to-areaは高速部分、area-slowは減速後に分離した（過去の同名区間と範囲が異なる）。
最終青を検知できず予測位置+200mmを超えたら停止する保険を追加。
新規診断は停止後に出力。高速化の実機検証はまだ。ビルド成功。

---

## 最新：Delivery通しへ統合（実機確認待ち）

ログ15-34-56は前回分も含むため00:33以降のみ評価。
ライン上/右1cmずらしの900mm時間はA93=2299/2365ms、
B95=2354/2365ms、C97=2266/2277ms、D93減速1.7=2299/2299ms。
全条件Kp0.30。Cは入口で振れるが復帰後の誤差7.79/6.92。
単体比較を一区切りにし、速度優先でCを通し確認する。

- Deliveryの曲線単体版をexperiments/delivery-curve-before-integration.*.disabledへ保存。
  HEADの通し版を基礎に統合。Tは4条件の直線テストのまま。
- 曲線はPWM93/Kp0.38→回頭-55度または500mmでPWM78/Kp0.55。
  青1本目の上も78を維持。追加加速・積分リセットはしない。
- 青1後の持ち替えからコーナー手前、配置後から帰りコーナー手前、
  帰りコーナー後の3区間はPWM97/Kp0.30/Ki0.01/Kd0.02/減速2.3。
  各高速区間の開始時にPID履歴をリセットする。
- コーナー手前の減速位置・PWM65は維持し、その時点でKp0.38へ戻す。
  行きコーナー後からエリアの短い区間は従来のPWM/ゲイン・エリア減速を維持。
  青確定70ms/最終50ms、彩度65、通過400msも変えない。
- 中断後に後続動作へ進まないよう段階間確認を追加。
  配置の斜め移動/旋回タイムアウト、後退の距離未達は中断扱い。

### 今回の診断

[DTrace]は曲線・行き高速・行きコーナー後・帰り高速・帰りコーナー後に分離。
直線は入口150mm/以降の時間、平均絶対反射率誤差・操舵量（100倍整数）、
白黒最大連続、出力頭打ち回数、開始方位からの変化範囲（10倍整数）を残す。
開始方位はラインに対する絶対角度ではない。左エッジの帰りは今回が初確認。
曲線ログは青通過または踏み越え検知までで、持ち替え旋回を含めない。

[BlueRuns]は実制御の読み取りから、条件に合う連続区間の開始時刻・車輪距離・
サンプル数・最初から最後のサンプルまでの時間/距離・最大彩度を固定24件まで保存。
診断用の追加センサー読み取りはしない。24件を超えるとdroppedを表示する。
区間数は物理的な青線本数ではなく、1本の線も読み取りが途切れれば複数に分かれる。
既存の青確定ログとentryNeed/finalNeedを照合して取りこぼしの余裕を見る。
車輪距離は後退関数でリセットされるので、往路と復路を直接差し引かない。

追加集計は往路停止後/帰還停止後に出力し、走行中の周期ログは増やさない。
往路診断出力の停止時間は[Delivery] outboundDiagに分離して残す。
総時間からこれを引いて比較する（既存イベントログの影響まで除けるわけではない）。
固定診断バッファは4KiBのタスクスタックを避け静的領域へ置き毎回初期化。
Deliveryは単一タスクで実行する前提。

まずBモードでボトル接近から配置・帰還まで通す。入口だけ93へ落とす制御はまだ入れず、
入口150mmの乱れ・時間、青連続一致の余裕、3区間の実時間を見て次の変更を決める。
ビルド・差分の空白検査は成功。実機未検証。

---

## 最新：追加比較2回目（PWM再評価）

前回ログ：pybricks-data-2026-09-19T15-24-40.csv。
1周目はほぼライン上、2周目は約1cm横ずれ。同じ周内で開始条件を揃えたとのこと。
全8本完走。900mm時間は旧A(0.38/2.3)2926/3312ms、
旧B(0.30/2.3)2552/2563ms、旧C(0.45/2.3)3047/3784ms、
旧D(0.38/1.7)2266/3091ms。
旧Bは150mm以降の平均絶対誤差8.51/7.70と、ずれた開始でも改善。
旧Dは5.47/26.64で開始条件依存が大きい。各状態1本なので確定ではない。

次はKp0.30、Ki0.01、Kd0.02、右エッジを固定し、以下に更新した。

| 新条件 | PWM | 減速係数 |
|---|---:|---:|
| A（旧Bの再確認） | 93 | 2.3 |
| B | 95 | 2.3 |
| C | 97 | 2.3 |
| D | 93 | 1.7 |

900mm停止・手動押下解放で次条件へ進む方式と安全打ち切りは維持。
入口150mmの誤差・操舵・方位変化の範囲と時間、150mm以降の方位変化の範囲、
初回制御の反射率、開始方位と終了時方位差を停止後ログへ追加。
方位は開始時との差であり、ラインに対する絶対角度や横ずれ量は推定できない。
150mm以降は分析用の区間名で、復帰完了を保証するものではない。

推奨は1周目ラインに平行で線上、2周目平行のまま同じ側へ約1cmずらす。
余裕があれば3周目は横ずれを加えず、同じ側へ約5度傾けた状態で比較する。
開始状態・ずらす方向は手動で記録する。各周内は条件を揃える。
結果を見て設定を選んだ後、直線3区間を含む通し走行へ進む。
今回もDelivery・共有制御には変更なし。ビルド成功、実機確認待ち。

---

## 2026-09-20 追記：直線の追加最適化（現在のTモード）

従来の結論は下に履歴として残す。PWM93でのゲイン比較が未実施だったため、
今回と次回程度に絞って追加比較する。速度改善が繰り返し確認できなければ他区間へ移る。

| 条件 | PWM | Kp | Ki | Kd | 減速係数 |
|---|---:|---:|---:|---:|---:|
| A 基準 | 93 | 0.38 | 0.01 | 0.02 | 2.3 |
| B P弱め | 93 | 0.30 | 0.01 | 0.02 | 2.3 |
| C P強め | 93 | 0.45 | 0.01 | 0.02 | 2.3 |
| D 減速弱め | 93 | 0.38 | 0.01 | 0.02 | 1.7 |

すべて右エッジ。フォースセンサー押下・解放で即PWM93のトレースを開始し、
車輪平均距離900mmで停止する。ボトル処理・転回・曲線走行は行わない。
A→B→C→D→A…と手動で繰り返す。毎回同じ直線の同じ位置・向きへ置き直す。
可能なら2周（計8本）採る。PIDと減速フィルターは毎回初期化する。

停止後に900mm全体の実時間、150mm以降の実時間と距離、平均絶対誤差、
平均絶対操舵量、出力頭打ち回数、白黒最大連続を出す。全体時間を主指標とし、
150mm以降の値で入口と定常区間を分ける。診断用の周期ログは追加しない。
中央ボタン、開始方位から20度超、白または黒400ms継続、10秒経過で打ち切り停止。
打ち切った条件は高速化成功として扱わない。自動復帰走行は行わず次の手動開始を待つ。
これらは逸脱を完全に防ぐ保証ではないので、直線終端には停止後の余裕も確保する。

変更はTのテストと文書のみ。Deliveryおよび共有PID/Tracerの設定は変えていない。
旧曲線テストの関数は残してあるが、現在のTからは呼び出さない。
実機結果は未取得。次回は結果に応じてD項や有望条件の再確認に絞る。

---

ETロボコン2026の走行体（LEGO SPIKE Prime + SPIKE-RT / TOPPERS ASP3、C++）。
単一の反射率センサーでエッジを追うライントレースを、**直線区間で速くする**ための調査記録。
2026-09-19 の測定（計30区間以上）で結論が出たので、その内容をまとめる。

## 結論（先に）

**基準PWMを100から93に下げるだけで、速度が約30%上がり、追従の乱れが5分の1になった。**
PIDゲインもカーブ減速も、Configの値も一切変えていない。

| | 変更前 PWM100 | 変更後 **PWM93** |
|---|---|---|
| 平均速度 | 615〜635 °/秒 | **788 / 831 °/秒** |
| 平均\|誤差\| | 25〜29 | **5 / 6** |
| 白黒の端への張り付き | 最大 白26・黒24 | **0 / 0** |
| 900mmの所要時間 | 2860〜3030 ms | **2300 / 2454 ms** |

理由は「PWM100では操舵が出力上限に当たって、狙った左右差が出ていなかった」ため。詳細は後述。

DeliveryTaskでは、直線を走る3か所（`kCornerTracePwm` / `kReturnTracePwm` / `kReturnAfterCornerFastPwm`）を93にした。

---

## 1. 調べ方

直線を **右エッジで900mm** 走る → **その場で180度転回** → **左エッジで900mm** 戻る、という往復テストを作った。
区間ごとに `Tracer::setConfig()` と `setCurveDecelGain()` で条件を差し替えるので、**1回の走行で2条件が同じ直線の上で取れる**（電池残量やコース差が入らない）。条件の割り当ては入れ替えられるようにして、左右エッジ・実行順との交絡を打ち消した。

制御周期（約11ms）ごとに集計し、100msごとに生値を出す。指標は5つ。

- **平均速度**［°/秒］（360°/秒 ≒ 176mm/秒）
- **平均\|誤差\|** … \|反射率 − 60\|。取り得る最大は約40
- **符号反転回数** … 目標値を跨いだ回数。横方向の振れ幅ではない点に注意
- **端への張り付き** … 白側・黒側それぞれの連続サンプル数の最大
- **PWMの頭打ち** … モーター側で±100に丸められたサンプル数（クランプ前の指令値をTracerから読む）

### 測り方で直した2点

- **立ち上がりを除外。** 180度転回の直後は車体が線からずれた位置から始まるため、復路は最初の100〜200mmが線の掴み直しになる。混ぜると復路だけ不当に悪く出るので、**最初の150mmは集計しない**（方位の基準もそこで取り直す）。
- **逸脱したら打ち切り。** 白か黒に400ms張り付く、または方位が20度ずれたらPWM40まで落とし、500ms戻らなければ停止する。減速を切った条件では線を外すとそのまま走り去るため。しきい値は成功走行の実測値（張り付き最大24、方位最大11度）の外側に置いた。

---

## 2. 対象の実装

```cpp
// Tracer::run() ── 約11ms周期（PIDは10ms固定で計算している）
float turn = -pid.calculate(robot.getReflection(), 0.01f);

float turnMag = fabs(turn);
filteredTurnMag = 0.05f * turnMag + 0.95f * filteredTurnMag;   // 時定数 約200ms

float minPwm   = basePwm * 0.2f;
float curvePwm = basePwm - curveDecelGain * filteredTurnMag;   // 既定 2.3
clamp(curvePwm, minPwm, basePwm);

pwm_l = curvePwm - edge * turn;   // モーター側で±100に丸められる
pwm_r = curvePwm + edge * turn;
```

PIDは `kp*偏差 + ki*積分 + kd*微分`。微分には α=0.10 のローパス、積分は±100でクランプ。
基準ゲインは kp 0.38 / ki 0.01 / kd 0.02、目標反射率60（白99・黒15）。

---

## 3. 測定の経過

### ラウンド1：基準設定のまま（PWM100・減速2.3）

平均620°/秒（≒305mm/秒）しか出ていない。反射率は `99 → 19 → 97 → 28` と**白黒の端を往復**していて、目標60の付近にいる時間がほとんどない。毎秒約3往復、約100mmごとに線を跨いでいた。比例制御というより実質オンオフ動作。

### ラウンド2：PIDゲインを振る → 失敗

| kp / ki / kd | 平均速度 | 平均\|誤差\| |
|---|---|---|
| 0.38 / 0.01 / 0.02（基準） | 656〜662 | 21〜24 |
| 0.18 / 0 / 0.06 | 544 / 648 | 25 / 19 |
| 0.25 / 0 / **0.10** | **263** | 24 |
| 0.10 / 0 / **0.10** | **348** | 24 |

**微分を上げると壊滅的に遅くなった。** 反射率は10msで20〜40も飛ぶので微分の生値は数千のオーダーになり、それが `turn` に乗って `filteredTurnMag` が張り付き、カーブ減速が下限（基準の20%）に貼り付く。実際 kd=0.10 の区間では片輪がほぼ停止したまま進んでいた。
※これは「今の減速方式との組み合わせ」の結果であって、D項が不要という結論ではない。

### ラウンド3：カーブ減速を切る → 速いが雑

| ゲイン | 減速 | 平均速度 | 平均\|誤差\| | 頭打ち |
|---|---|---|---|---|
| 基準 | 2.3 | 640 / 719 | 24 / 19 | 4〜5% |
| 基準 | **0** | **910 / 928** | 26 / 20 | — |

1.4倍速くなった。ただし平均誤差は改善せず、**基準PWMを85まで下げても6〜8割のサンプルで頭打ち**のままだった。減速を切ると蛇行が大きいので操舵量自体が大きく、基準PWMを下げても追いつかない。**減速0の速さは追従の結果ではなく、蛇行しながら押し切っているだけ**と判断して、この方向は打ち切った。

なお比例0.18・減速0では**440mmで線を外して走り去った**（方位−125度）。減速を切るなら比例は下げてはいけない。

### ★出力上限に当たっていた

`Tracer` の出力は `基準PWM ± 操舵量` だが、モーター側（pbio）では**duty が±100に丸められてから電圧に変換される**（`pbio_battery_get_voltage_from_duty_pct` が最初にクランプする）。100%は「電池の電圧を全部モーターに掛ける」という意味なので、**その先に余裕はない**。設定で上げることもできない（`setDutyLimit` も0〜100で、下げる方向の機能）。

```
基準100・操舵20 → 計算上 80/120 → 実出力 80/100   （左右差40が20に縮む）
```

実測でも、減速1.0のとき**6割のサンプルでクランプ**していた（指令の最大119）。
つまりPWM100では「狙った左右差が出ない → 線を外す → 蛇行する → カーブ減速が効き続ける」という循環に入っていた。

### ラウンド4：減速は基準のまま、基準PWMだけ振る

| 基準PWM | 平均速度 | 平均\|誤差\| | 端への張り付き 白/黒 | 頭打ち |
|---|---|---|---|---|
| 85 | 680 / 653 / 656 / 728 | 10 / 11 / 11 / 6 | 0〜6 | 0 |
| 90 | 654 / 698 / 772 | 14 / 12 / 7 | 4〜7 | 0〜3 |
| **93** | **788 / 831** | **6 / 5** | **0 / 0** | 0 |
| 95 | 818 / 865 / 638 / 817 | 9 / 10 / 18 / 7 | 0〜7 | 0 |
| 97 | 604 / 623 | 24 / 23 | 5〜8 | 0 |
| 100 | 635（+ 初期の615前後） | 26 | 9〜24 | 0〜5 |

**93〜95が最良で、97以上は急に悪化する。** 100から数だけ下げると、速度が約200°/秒上がり誤差が数分の1になる。
93は2本とも端への張り付きゼロ・誤差5〜6と最も安定していた（95は3本中1本が638°/秒・誤差18に落ちた）。
100msごとの生値も別物になっている。

```
PWM100： refl 99 → 19 → 97 → 28 → 61 → 82 → 26 …
PWM93 ： refl 61, 59, 57, 53, 52, 52, 63, 68, 53, 65, 60, 58 …
```

※97と100は復路（左エッジ）、93と95は往路（右エッジ）での測定。93と95は同じ土俵なので直接比較できる。

---

## 4. 採用した変更

`main/app/tasks/DeliveryTask.cpp` の3つの定数を 100 → 95。

| 定数 | 区間 |
|---|---|
| `kCornerTracePwm` | 行き：青1本目の後 〜 90度コーナー手前の減速まで |
| `kReturnTracePwm` | 帰り：エリアを出てから減速まで |
| `kReturnAfterCornerFastPwm` | 帰り：90度コーナーの後 〜 終了までの900mm |

`Config` の値も `Tracer` の既定動作も変えていないので、**他のタスク（ラリー・スモウ）への影響はない。**

共有コードへの追加は2つだけ。どちらも呼ばなければ従来通り。

- `Tracer::setCurveDecelGain()` … カーブ減速の強さを一時的に変える（既定値は `Config::TRACER_CURVE_DECEL_GAIN`）
- `Tracer::getLastLeftPwm()` / `getLastRightPwm()` … クランプ前の指令値を読む（診断専用）

`GameRunner` にモード4「B」（デリバリーのみ。LAPまでのトレースもラリーも通さない）を追加した。既存モードの流れは変えていない。

### ラウンド5：わざと線からずらしてスタート（最終確認）

本番では曲線を抜けた直後に直線へ入るので、毎回きれいな姿勢で入れるとは限らない。そこで**線から横にずらして置いてスタート**し、立て直せるかを見た（いずれも往路・右エッジ・立ち上がり150mmは集計外）。

| 条件 | ずれ | 平均速度 | 平均\|誤差\| | 端への張り付き |
|---|---|---|---|---|
| PWM95 | 約1cm | 638 | 18 | 白7・黒6 |
| **PWM93** | 約1cm | **786** | **5** | **0 / 0** |
| PWM93 | 約1.5cm | 590 | 19 | 白5・黒7 |

**1cmのずれからは93だけが完全に立て直した**（誤差5・張り付き0は、きれいにスタートしたときと同じ数字）。
95は同じ1cmで立て直せないまま750mmを走り切った。

1.5cmになると93でも戻らない。**この速度域で復帰できるのは1cm程度まで**、というのが現状の限界。
（各条件1本ずつなので、傾向として扱う）

---

## 5. 分かったことと、残っていること

**分かったこと**

1. 直線での速度を食っていた主因は、PIDゲインではなく **出力上限と、それが呼ぶ蛇行・カーブ減速の循環**だった。
2. 基準PWMは「上げるほど速い」ではない。**操舵に使う余裕を残した93〜95が最速**で、97以上では元に戻る。
3. 微分を上げるのは今の減速方式とは相性が悪い（|操舵量|を通じて速度を潰す）。
4. **きれいに入れば95が少し速い**（842 vs 810°/秒、約4%）が、**1cmずれて入ると93だけが立て直せた**。本番は曲線から直線へ入るので、余裕のある93を採った。
5. 1.5cmずれると93でも戻らない。入口の姿勢は速度と同じくらい効く。

**残っていること**

- **曲線では未検証。** 95は直線で決めた値なので、当面は「直線区間だけ95」。
- 実周期は約11ms。PIDは10ms固定で計算しているので、微分と積分が1割ほど実態とずれている。
- 反射率が端まで振り切る現象そのものは、PWM95で大幅に減ったが原理的には残っている。
- 速度が上がると青の連続判定や停止位置も変わる。直線単体の成功だけで完了にしない。
