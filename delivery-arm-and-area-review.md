# デリバリー：ボトルの色判定・青1本目後の移動・エリアでの停止

## 2026-09-22：下降を上昇と同じ速度の位置制御へ統一

Deliveryのlower-retry／lower-finalは、既存のpositionDeliveryArmを使い、速度を
ARM_RAISE_SPEED_DEG_PER_SECの絶対値（現在1000度/秒）へ揃えた。従来の下降速度は600度/秒。
目標は初回上げ前のarmHomeCountのまま。加減速・保持・動作上限と共有の下降設定は変更なし。
下降未達は従来どおりログだけ残して続行する。ボタン確認やUNKNOWN終了は変更していない。
既存ホスト試験7本通過。下降動作そのものの実機確認は未実施。

## 2026-09-22：アーム下降後の固定移動をRコースへ対応

ユーザー確認により、左PWM35・右PWM40は車体補正ではなくL用の調整値。
`test/delitest4`で、Rの場合だけ左右を交換し左40・右35にした。
Lの出力、1秒の移動時間、色通知、その他の制御は維持。実機確認は未実施。
戻す場合はDeliveryTask.cppの当該setMotorPowerを左右固定の2定数へ戻す。

### 17-45-55 / 17-46-58の結果

初回アームは目標-74に対して-72、目標-72に対して-71でsettled。
ユーザー目視でもほぼ狙いの位置。動作中の最大出力は両方99%で、
以前の保持中5〜13%だけでは動作中の負荷を代表していなかったことが分かった。
上限を増やした結果ではなく既存制御出力の記録。音や滑りの原因をこれだけで断定しない。
アームと後退後の設定は据え置く。
青1の接続は95/97mm・649/638msで安定判定。まだ短縮候補だが今回は変更せず、
行きコーナー後の高速化と最終青250mm手前減速を先に実装した。
詳細はstraight-speed-review.md冒頭を参照。

## 最新更新：角度不足・接続姿勢を再開（2026-09-20）

**以下の過去の「角度は原因ではない／打ち切り」「弧移動は完了」という結論を撤回する。**
ユーザーが問題としているのは色判定の成否ではなく、上端より手前の所定姿勢まで
アームが上がっていないこと、上げ直し時に時々ガチガチ音がすること。
超音波・ボトル距離は対象外で、接近条件や追加距離対策は変更しない。

対象は17-21-25（リトライあり）と17-22-27（一発で読めたが角度不足）の2件。
初回読取時の出力は-6/-5%、リトライ読取時は-11%。動作中の出力ピークは未記録。
この点の低出力やstalled=noだけで引っかかり・ギヤ滑り・負荷変動を否定できない。
積算角度はモーター軸の比較であって、センサー実姿勢やギヤ滑りを測るものではない。
旧commitArmMovement方式は確定後もcoastで動けば誤差が出て、
確定直後のlogPostureでは同じカウントを二重加算する場合もあった。

### 実装した変更

1. **アーム：Delivery専用の非リセット位置制御。**
   初回上げ前のモーター位置を基準に、上げは基準-172度、下げは基準そのもの。
   リトライで毎回+2度ずつ押し上げる方式はやめ、同じ上げ・下げ位置を再使用する。
   上端への押し当てや出力上限の増加は行わない。実姿勢として172度が十分かは未確定。
   位置誤差±2度・速度20度/秒以下が5周期連続したら完了。
   2秒上限、失速連続検出、中央ボタンで打ち切り、後続走行へ進まない。
   未達時は現在位置で保持し、元の遠い目標へ押し続けない。
   上げ/下げ中に40msごとの角度・速度・出力、10msごとの最大絶対出力を集計し、
   動作終了後に[ArmMotion]/[ArmSample]で出す。最大51点。
   通常のraiseArm/lowerArm等は変更せず、他タスクは既存動作のまま。
   再試行5mmは従来通りだが、上げと同時でなく前進完了後に上げて条件を揃える。
   下げ側も位置制御/HOLDになるため、所要時間と実姿勢は再確認が必要。

2. **転回→帰路：固定踏み込みを反射率による接続に置換。**
   70度転回は最大PWM70を維持し、発進80msと終端20度でPWMを60〜100%に調整。
   転回終了後に既存の間引きブレーキで整定してから片輪探索する。
   [AreaTurn]に指令終了/整定後の方位差と全3軸の角速度ピークを追加。
   取り付け軸を未確認なので、どの軸も自動的にピッチとは断定しない。
   線発見後の固定30/90 PWM・100msの踏み込みを廃止する。
   最新ログでは帰路入口で-17〜-20度の方位修正があり、発見だけでは姿勢が整っていない。

3. **青1後と配置後：低速でエッジを掴んでから97へ。**
   線検出直後にPWM65/Kp0.30で接続し、20mm以上進み反射率60±15が8周期連続したら97へ移行。
   接続は最大150mm/1.5秒/開始から30度。成立しなければ停止し、高速へ移らない。
   [Handoff]に距離・時間・方位変化・開始/終了反射率を記録する。
   高速切替時はPID履歴を保持する。コーナー減速距離の原点は接続開始前とし、
   低速接続ぶんだけ減速位置が先へずれないようにした。
   [DTrace]高速区間の原点は接続後になるので、過去比較ではHandoffも合わせて評価する。

**弧移動の幾何形状65/39・黒35は今回維持。**
2本とも弧で約5度、25〜27mm移動した後に直線入口で大きく振れており、
弧単体が110msに縮んだだけでは成功としない。まず接続までの総距離・時間・
姿勢を評価する。線の反対側へ移る動作を推測だけで削除しない。
弧がタイムアウトして黒を確認できない場合は、97へ進めず停止するよう修正。
後退75mm/デューティ60は維持。共有brakeの仕様も今回変更しない。

### 次の実機で確認すること

- 色が読めても所定のアーム姿勢かを目視。足りなければ実姿勢とモーター移動量を対応させる。
  音が出たのが上げ/下げ/保持のどこかも記録。ガチガチ音が続く場合は中断し機構を確認。
- ArmSampleの初回/再試行の出力ピーク・角度の停滞・速度反転を比較。
  エンコーダーが目標でも実姿勢不足なら、目標角・初期位置・ギヤ/リンクの遊びを確認する。
- AreaTurnの指令終了と整定後の差、Handoffの成立と移動量を比較。
- 青1後は弧だけでなくHandoff＋直線入口の合計で評価。65は調整用で最速値ではない。

ビルド成功。実機未検証。診断ログは移動後に出すが、その出力待ちは全体時間に入るため、
この版の全体時間だけで高速化を評価しない。

---

ETロボコン2026 / LEGO SPIKE Prime + SPIKE-RT（TOPPERS ASP3, C++）。
単一の反射光センサーによる片エッジのライントレース。制御周期は約11ms、移動系のポーリングは10ms。

ライントレース区間の高速化は別文書（[curve-section-review.md](curve-section-review.md)）で扱っている。
**この文書は、走行以外の3つの問題**が対象。

---

## 0. 3つの問題と現在地

| # | 問題 | 現在地 |
|---|---|---|
| **1** | ボトルの色判定が1回目に失敗する | **切り分け完了。角度は原因ではなかった（§1.7）。対策は打ち切り** |
| 2 | 青1本目の後、右エッジへ移る弧移動が遅い | **完了。**約200ms → **110〜121ms / 25〜29mm** で安定 |
| 3 | エリアでの停止・後退で車体が浮き、後退がオーバーランする | **行き過ぎは解決・計測可能に。**「浮き」は未計測のまま |
| 4 | アーム前の静止待ちが毎回550msタイムアウトしていた | **完了。**165〜176msで成立するようになった |

**直近2本の全体時間：26784 ms（リトライなし）／ 29855 ms（リトライ1回）。**

> **前提の訂正（重要）。**調査の途中で「ボトルまでの距離が原因」という強い相関が出たが、
> **これは試験環境の問題だった。**試験ではボトルだけが本番と違うものを使っており、本番環境では読めている。
> **距離の話はこの文書の対象から外す**（記録として§1.3に残すが、対策はしない）。
> **残る論点はアームの角度とトルクだけ。**

---

## 1. 問題1：1回目の色判定が失敗する

### 1.1 症状

ボトルの手前で止まり、アームを上げて、アームに付いたカラーセンサーでボトルの色を読む。
**1回目の読み取りが失敗することが多い。**失敗すると、アームを下げ、5mm前進しながら上げ直して読み直す（約1.9秒）。

```
Bottle reading: h=0 s=0 v=1 refl=1      ← 1回目。真っ暗で色が出ない
Bottle color unknown. Retry 1/5: advance 5mm while raising to 172 deg
Bottle reading: h=352 s=88 v=15 refl=5  ← 2回目。読める
```

### 1.2 外した仮説が2つある

**仮説A：車体が止まりきる前にアームを上げているので、車体のピッチが違う。**

接近ループは `tracer.terminate()`（＝coast）で抜けるだけで、停止を待っていなかった。
実測でアームを上げ始めた時点のモーター速度は**左307 / 右285°/秒**。

→ **停止と整定待ちを入れた。**これは入れる価値があった（後述）が、**これだけでは直らなかった。**

**仮説B：位置制御が目標角に届く前に「完了」と言っているので、アームが低い。**

```
Arm raise: settled at 165 deg (target 170, reached)   ← is_done()は165で真になる
Arm angle at color reading: 171 deg                    ← 200ms後には171まで来ている
```

実際、最初に成功した回は読み取り時171、失敗した回は167だった。**そこで「実際に目標角に届くまで待つ」処理を入れた。**

→ **外れ。**次のデータで否定された。

| ログ | 読み取り時のアーム角 | v | 結果 |
|---|---|---|---|
| 16-45-41 | **171** | 20 | 成功 |
| 16-44-27 | **171** | 5 | **失敗** |

当初これを「同じ171度で成功と失敗が出たので、角度では説明できない」と書いていたが、**この読み方は誤り。**

**ログの角度は姿勢ではなく「その動作での移動量」。**`raiseArm()` / `lowerArm()` は動作の先頭で
`arm.resetCount()` しているため、値は毎回その動作の開始位置からの相対値になる。

```cpp
bool startArmRaise(...) {
    ...
    arm.resetCount();   // ← 動作ごとに原点が変わる
```

**開始位置が違えば、同じ171度でもセンサーの向きは違う。**実際、上げ165に対して下げ174のように
毎回閉じておらず、開始位置は動いている。**仮説Bはまだ否定できていない。**

→ **動作を跨いで積算した角度を記録するようにした**（§1.6）。これがないと1回目と2回目を比較できない。

### 1.3 距離との相関（記録。ただし試験環境固有と判明したため対策はしない）

**アームを上げる直前の超音波センサーの値と、読み取れた明度(v)が、4本すべてで単調に対応していた。**

| ログ | アーム上げ直前の超音波[mm] | 1回目のv | 1回目の彩度 | 結果 |
|---|---:|---:|---:|---|
| 16-23-46 | **85** | **52** | 93 | 成功 |
| 16-45-41 | **93** | **20** | 93 | 成功 |
| 16-44-27 | **102** | **5** | 0 | **失敗** |
| 16-24-58 | **104** | **1** | 0 | **失敗** |

**93mm以下なら読め、102mm以上なら読めない。**
明度は 85→52 / 93→20 / 102→5 / 104→1 と、距離が20mm延びるだけで50分の1になる。
反射光は距離とともに急激に落ちるので、物理的にも素直な説明になる。

**そして、リトライが効く理由もこれで説明がつく。**リトライは5mm前進する。
102mm → 約97mm、104mm → 約99mm になり、どちらもその回は読めている（v=13 / v=15）。
**アームを上げ直しているから読めたのではなく、5mm近づいたから読めていた。**

（なお、停止と整定を入れる前の2本もvは1で失敗しているが、**当時は読み取り中も車体が動いていた**ので、
この超音波の値は読み取り時の距離を表していない。上の表からは除いてある。）

**この相関は試験環境固有だった。**試験ではボトルだけ本番と違うものを使っており、本番環境では同じ距離でも読めている。
**距離に対する対策は行わない。**以降は角度とトルクだけを追う。

### 1.4 残っている論点：角度が効いているのか、1回目そのものが悪いのか

距離を除くと、**1回目と2回目の違いは2つしかない。**

| | 1回目 | 2回目（リトライ） |
|---|---|---|
| **上げ角の目標** | **170度** | **172度** |
| 上げ方 | `raiseArm()`（停止状態から） | `raiseArmWhileDriving()`（5mm進みながら） |

そして**止まる角度も毎回違う。**

| ログ | 1回目：完了時 → 読取時 | 2回目：完了時 → 読取時 |
|---|---|---|
| 16-23-46 | 165 → **171** | （1回目で成功） |
| 16-24-58 | 166 → 167 | 167 → 167 |
| 16-44-27 | 165 → **171** | 169 → 170 |
| 16-45-41 | 167 → **171** | （1回目で成功） |

位置制御の `pbio_control_is_done()` は**目標に届く前に真になる**（165〜167で「reached」と言う）。
その後の保持で上がっていくが、**どこまで上がるかが一定しない。**

### 1.5 方針：1回目から2回目の角度で上げる

**変数を1つ減らす。**

```cpp
// 変更前： int armExtraDeg = 0;                        → 1回目の目標 170度
// 変更後： int armExtraDeg = kBottleRetryExtraArmDeg;  → 1回目の目標 172度（＝2回目と同じ）
```

**ただし「成功＝角度が原因、失敗＝角度ではない」という二択にはならない。**
上のとおり目標角を2度上げても、**開始位置が違えば実際の姿勢の変化は2度ではない。**

そのため、この試験は**角度の効果を測るものではなく、「2回目と同じ条件にしたら通るか」を見るもの**として扱う。
角度そのものの効果を見るには、**積算角度（§1.6）で実際の姿勢の差を確認する**必要がある。

**リトライの仕組みは保険として残す。**

### 1.6 トルクをモニタできるようにした

「1回目と2回目で力のかかり方が違うのではないか」という見立てを確かめるため、
**モーターの出力と失速判定**を読めるようにした（`Robot` への追加のみ。既存の挙動は不変）。

```cpp
int getArmPower() const;    // アームモーターの出力[%]
bool isArmStalled() const;  // 出力を出しているのに回っていない状態か
```

**`getPower()` は電流やトルクの実測ではなく、印加電圧から換算した出力率。**
保持中は速度がほぼ0なので負荷の比較には使えるが、**トルクそのものではない。**

**あわせて、動作を跨いで積算したアームの角度も記録するようにした。**

```cpp
// 各raiseArm/lowerArmの直前に、直前の動作ぶんを確定させる
void DeliveryTask::commitArmMovement() { armAbsoluteBaseDeg += robot.getArmCount(); }
// 1回目の上げ直前を原点とした積算角度（負が上げ側）
int DeliveryTask::armAbsoluteDeg() const { return armAbsoluteBaseDeg + robot.getArmCount(); }
```

**これで初めて、1回目と2回目のセンサーの向きを比較できる。**

出るログ：

```
[Posture] before raise 1: arm power NN %, stalled no
[Arm] reached 170 deg (target 172) in NN ms, power NN %, stalled no
[Arm] STOPPED SHORT at 167 deg (target 172) after 400 ms, power NN %, stalled YES
```

**見るべきこと：**

| 観測 | 見立て（いずれも断定はできない） |
|---|---|
| `STOPPED SHORT` ＋ `stalled YES` ＋ 出力が高い | 引っかかりの**疑い**。角度を上げても解決しない可能性 |
| `STOPPED SHORT` ＋ 出力が低い | 保持制御がそこで釣り合っている**可能性**。目標誤差や制御状態も要確認 |
| `stalled no` | **引っかかりがないことの証明にはならない** |

**1回目と2回目で出力が違えば、「力のかかり方が違う」という体感の裏づけにはなる。**
ただし `pbio_control_is_done()` の完了判定は**位置と速度が許容範囲に入ったこと**であって目標角への一致ではないので、
165〜167で完了すること自体は直ちに不具合ではない。

**なお、直近2本は目標170度の旧条件で取ったものであり、出力のログも入っていない。
172度とトルクについてはまだ何も検証できていない。**

### 1.7 結果：目標172度でも1回目の失敗は残り、順序も角度では説明できない

積算角度が出るようになってからの4本。

| ログ | アーム上げ直前の超音波 | 1回目の積算角度 | 1回目 彩度 / 明度 | 結果 |
|---|---:|---:|---|---|
| 17-05-39 | **81 mm** | −172 | **89 / 66** | 成功 |
| 17-06-57 | 99 mm | −170 | 86 / 7 | 成功 |
| 17-22-27 | 99 mm | −173 | 92 / 7 | 成功 |
| **17-21-25** | **106 mm** | **−174** | **0 / 5** | **失敗** |

**角度では順序がつかない。**失敗した回が**最も深く上がっている**（−174）。
同じ走行のリトライは**−172と浅い方**で成功している（彩度96）。
「角度が足りないから読めない」という仮説は、このデータでは支持されない。

**一方、超音波距離とは相変わらず対応している。**81→明度66、99→7、99→7、106→5。
**§1.3で見つけた相関が、172度に上げた後もそのまま残っている。**

**判定の実体は彩度。**明度は成功例でも7〜9しかない。
**彩度が閾値40を超えるかどうかが分かれ目**で、失敗した回だけ彩度が0に落ちている。
つまり**光量がぎりぎりで、色相が計算できるかどうかの境目にいる。**

### 1.8 結論と方針：この問題は打ち切る

- **角度は原因ではない。**目標172度に上げても失敗は出たし、失敗回の方が深く上がっていた。
- **トルクにも異常はない。**5本すべて出力−5〜−13%、`stalled` は一度も立たなかった。
  引っかかりの形跡はない。
- **支配的なのは距離。**ただし**これは試験環境固有**で、本番のボトルでは読めている（§0の注記）。

**したがって、この問題への対策はここで打ち切る。**

- **目標角は172度のまま残す。**3/4が1回目で成功しており、170度に戻す積極的な理由がない。
  ただし**172度が効いたという証拠もない。**
- **リトライは保険として残す。**試験環境では今後も発動する。
- **診断ログ（積算角度・出力・姿勢）は残す。**本番で同じ症状が出たときの一次情報になる。

**もし本番で再発したら、最初に疑うのは距離。**そのときは「アームを上げる前に超音波で距離を詰める」
（§1.5で検討した閉ループ）に戻る。

---

## 2. 問題2：青1本目の後の弧移動

### 2.1 何をしているか

青1本目を通過した後、左エッジのトレースから右エッジのトレースへ移る。
そのために弧を描いて線を横切り、**黒を踏んだ時点で止める。**

### 2.2 元の実装の無駄

```
move stop by black at 154 ms, reflection start 52
move stop by black at 132 ms, reflection start 59
```

**132〜154msのうち100msは固定の最低移動時間だった。**「動き出しで左エッジの黒を拾って即終了しない」
ための待ちで、実際に黒を探していたのは32〜54msだけ。

### 2.3 変更したこと

**懸念を時間で潰すのをやめ、開始時の状態で場合分けした。**

```
開始時が黒（反射率≤35が3サンプル） → すでに線の上。動かさない
開始時が45以上                     → 最低時間なしで動かす
開始時が36〜44（中間）             → 40msだけ下限を置く
```

あわせて**弧の速度を 50/30 → 65/39 に上げ、進んだ距離も記録**するようにした。

### 2.4 結果

| 設定 | 所要 | 進んだ距離 | 開始時の反射率 |
|---|---|---|---|
| 50/30（元） | 154 ms | （未計測） | 52 |
| 50/30（元） | 132 ms | （未計測） | 59 |
| 50/30（最低時間を外した直後） | 121 ms | （未計測） | 68 |
| 50/30（同上） | **275 ms** | （未計測） | 48 |
| 65/39（stop()を入れる前） | 110 ms | 27 mm | 60 |
| 65/39（同上） | 143 ms | 35 mm | 65 |
| **65/39（現在）** | **110 ms** | **25 mm** | 70 |
| **65/39（現在）** | **121 ms** | **29 mm** | 65 |
| **65/39（現在）** | **121 ms** | **27 mm** | 69 |
| **65/39（現在）** | **110 ms** | **25 mm** | 68 |

**約200ms → 110〜121ms。**275msのような外れ値は消え、**距離も25〜29mmに収束した。**
直前の出力を切るようにした（§2.5）後の4本は、時間も距離もばらつきが半分以下になっている。

### 2.5 修正：直前の出力が残ったまま判定・ログをしていた

**「動かさない」判定の3サンプル(約30ms)の間、直前の `tracer.run()` が出した出力が残っていた。**
判定中も実際には走っている状態だった。

**黒を検出してループを抜けた後も、ログを2行出す間は弧の出力が残っていた。**

→ **どちらも明示的に `robot.stop()` を入れた。**`stop()` はcoastなので惰性では進むが、
少なくとも駆動していない状態で判定・ログができる。

**しきい値をいじるのはこの後。**

### 2.6 残っている問題と方針

**距離25〜29mm、時間110〜121msで収束した。**当初の132〜154msから見ると、
**固定待ちの100msを消しても安全側は崩れていない。**この項目は完了扱いでよい。

これ以上詰めるなら残っているのは、

- **黒の閾値（35）を上げて早く止める。**右エッジのトレースが残りを引き取るので、線の中央まで入る必要はないかもしれない
- ただし**単一の反射光センサーでは線のどちら側にいるか分からない。**早く止めすぎて左側に残ると、
  右エッジのトレースは線から離れる向きに操舵する

**「開始時が黒なら動かさない」は、まだ一度も発動していない。**開始時の反射率は48〜68で、
線の上から始まった回がない。**この分岐が必要なのかどうかも、まだ分かっていない。**

さらに、**「黒を踏んだ＝正しい側へ移れた」という保証はない。**単一の反射光センサーでは
線のどちら側にいるか分からないので、この分岐は発動例が取れるまで慎重に扱う。

---

## 3. 問題3：エリアでの停止と後退

### 3.1 症状

ボトルをエリアに置く前後で、**車体が浮く。**また、**60mmの後退がオーバーランする。**

### 3.2 原因1：共有の `brake()` が「完全停止まで待つ」仕様に変わっていた

チームの別のコミットで、`turnByImu` にあった「モータが完全停止するまで待つ」処理が `Robot::brake()` へ移された。

```cpp
void Robot::brake() {
    leftMotor.brake();
    rightMotor.brake();
    while(stableCount < Config::BRAKE_STABLE_COUNT) { ... }  // ≤5°/秒になるまでブロック
}
```

一方、デリバリー側の整定処理は**わざと間引いて弱く掛ける**設計だった。

```cpp
// 掛けっぱなしにせず間引くことで、制動力を平均で下げる
if((i % 3) < 1) { robot.brake(); } else { robot.stop(); }
```

**`brake()` がブロックするようになったため、1回目の呼び出しで止め切ってしまい、間引く意味が消えて全掛けと同じになっていた。**
ログにも出ていた。

```
Settled in 0ms      ← ループ側の判定が一度も働いていない
```

→ **デリバリー側でモーターを直接叩くように変更**（共有ファイルは触らない）。
`Settled in 190ms / 200ms` と、意図どおり時間が掛かるようになった。

### 3.3 原因2：後退の指令速度が飽和していた

```cpp
constexpr int kAreaBackwardMm = -60;                // 60mmだけ後退する
constexpr int kAreaBackwardSpeedDegPerSec = 10000;  // 常に飽和させて最速で後退
constexpr int kAreaBackwardDutyLimit = 100;         // 100 = トルク制限なし
```

**60mmしか動かさないのに、指令速度を飽和させていた。**実測のモーター速度は後退直後で **-667 / -716°/秒**。
ホイール半径28mmなので**約330mm/秒**。10msのポーリングでは1周期で3mm進み、
**そこから止まるまでの距離がそのまま行き過ぎになる。**

デューティ上限のつまみはこのために用意されていたが、100（無効）のままだった。

→ **60に変更。**

### 3.3.1 訂正：行き過ぎの本体は「指令の終わり方」だった

当初この文書には「`driveStraight` の末尾にも `brake()` が入る」と書いていたが、**誤り。**
`Robot` には2つある。

| 関数 | 末尾 |
|---|---|
| `driveStraight()` ← **後退で使っているのはこちら** | **`stop()`（coast）** |
| `driveStraightByImu()` | `brake()`（完全停止まで待つ） |

**エンコーダが60mmに達した時点で駆動を切るだけで、そこから先は惰性。**
デューティ上限で最高速度を下げれば惰性も短くなるので改善はするが、**原因そのものは指令の終わり方。**

→ **後退の直後に間引きブレーキで止め切るようにした。**あわせて、
**指令が終わった時点と、止まり切った時点の車輪距離**を記録するようにした。

```
Backward: commanded -60 mm, at command end NN mm, after stop NN mm
Backward: overrun after command NN mm
```

**これで行き過ぎ量が数値で出る。**

### 3.4 結果

| | デューティ上限100 | デューティ上限60 |
|---|---|---|
| 後退中のモーター速度（ログ上の観測値） | -667 / -716 °/秒 | -100 / -164 / -431 / -495 °/秒 |
| 整定の所要時間 | 0 ms（判定が働いていない） | 150 / 160 ms |

**行き過ぎ量が測れるようになった結果（指令を75mmに増やした後）：**

```
Backward: commanded -75 mm, driven -76 mm, overrun -10 mm, total -86 mm
Backward: commanded -75 mm, driven -75 mm, overrun -11 mm, total -86 mm
```

**2本とも実効86mm、惰性は10〜11mm。**指令に対して13〜15%で、**再現している。**
後退量を60→75mmに増やしても、**旋回後のライン探索は成立している**（`Line found by reflection: 51 / 54`）。

**行き過ぎの問題は解決とみなしてよい。**予測できる値になった。

なお**モーター速度はログの疎なサンプルでしか見ていない**ので、最高速度がどれだけ下がったかは確定できない。
ただし**行き過ぎが10〜11mmに収まっている**ことは直接測れているので、実用上はこれで足りる。

### 3.5 残っている問題：「浮き」は未計測のまま

**行き過ぎは解決したが、「浮く」は直接測れていない。**目視でしか判断していない。

IMUの加速度と角速度を読む口は追加した（`getImuAcceleration()` / `getImuAngularVelocity()`、
どちらも追加のみで既存の挙動は不変）。**後退中と旋回中のピッチ方向の角速度のピークを記録すれば、
浮きを数値で比較できる。**現状は区間の境目でしか記録していない。

**次に必要な計測：**

- **指令終了時と停止後の車輪距離の差**（＝行き過ぎ量）← 追加済み
- **後退中・旋回中のピッチ角速度のピークと、短時間の角度変化**
- **目視の浮きと、上の数値の対応**

**動作中の加速度から単純に傾きを出すのは避ける。**重力の分解として読めるのは静止時だけ。
**角速度だけでも「車輪が浮いた証拠」にはならない**ので、両方と目視を突き合わせる。

まだ触っていない候補：

- **後退の指令速度そのものを下げる**（今は10000で飽和）。60mmに飽和指令は過剰
- **後退の立ち上がりを段階的に上げる。**斜め移動にはすでに同じ仕組みが入っている（`kDiagonalRampPwms`）
- **その場旋回の±70PWMも階段状の指令。**ボトルは置いた後なので優先度は低い

### 3.6 静止待ちが毎回タイムアウトしていた

アームを上げる前の整定待ちは `IMU::isStationary()` を条件にしていたが、
**直近2本とも550msのタイムアウトまで一度も真にならなかった。**

```
Settled before arm raise: 550 ms (timeout)
```

**有効な静止検出になっておらず、ただ550ms待っているだけだった。**判定窓が長いとみられる。

→ **角速度そのものを見るように変更。**全軸3°/秒未満が5回続いたら収まったとみなす。

**結果：**

```
変更前: Settled before arm raise: 550 ms (timeout)         ×4本
変更後: Settled before arm raise: 165 / 165 / 165 / 176 ms (gyro quiet), imu stationary no
```

**約385msの短縮。**そして `imu stationary no` が4本すべてで出ており、
**`IMU::isStationary()` はこの場面では最後まで真にならない**ことが確定した。判定窓が長いとみられる。

---

## 4. 追加した計測

| ログ | 内容 |
|---|---|
| `[Posture] <label>: accel x/y/z, stationary` | 車体の姿勢。静止していれば重力の分解になる |
| `[Posture] <label>: gyro x/y/z, arm N deg` | 角速度とアーム角度 |
| `Settled before arm raise: N ms (reason)` | 停止後、IMUが静止と言うまでの時間 |
| `[Arm] reached N deg (target N) in N ms, power N %, stalled` / `STOPPED SHORT` | 保持で目標角まで届いたか、そのときのトルクと失速 |
| `[Posture] <label>: arm power N %, stalled` | アームモーターの出力と失速判定 |
| `[Blue1] move stop by <reason> at N ms / N mm` | 弧移動の所要時間と距離 |

**足りていないもの：**

- **上げている最中のアーム角度と出力の連続記録**（今は完了時と読み取り時の点でしか見ていない）
- **後退中・旋回中のピッチ角速度のピーク**

---

## 5. 現時点のまとめと、次にやること

### 片付いたもの

| # | 内容 | 効果 |
|---|---|---|
| 2 | 弧移動の固定待ち100msを、開始時の状態による場合分けに置き換え。直前の出力も明示的に切る | **約200ms → 110〜121ms** |
| 3 | 後退の末尾が `stop()`（coast）だったので、間引きブレーキで止め切る。行き過ぎを計測 | **行き過ぎ10〜11mmで再現。後退量を75mmへ増やせた** |
| 3 | 共有の `brake()` がブロックする仕様に変わっていたため、整定が全掛けになっていた | **`Settled in 0ms` → 150〜200ms**（意図どおり効くようになった） |
| 4 | `IMU::isStationary()` が成立しないため、角速度で判定 | **550ms → 165〜176ms** |

**合計で約0.5秒。**リトライが出なければ全体は26.8秒。

### 打ち切ったもの

**問題1（アームの角度）。**§1.7・§1.8のとおり、角度は原因ではなかった。
支配的なのは距離で、それは試験環境固有と判明している。**診断ログだけ残して先へ進む。**

### 残っているもの

1. **「浮き」が未計測。**行き過ぎは数値で追えるようになったが、浮きは目視のまま。
   評価するなら**後退中・旋回中のピッチ角速度のピークと短時間の角度変化**が要る。
   動作中なので加速度から傾きを出すのは使えない。
2. **弧移動の黒しきい値。**35から上げれば更に縮むが、
   **単一の反射光センサーでは線のどちら側にいるか分からない。**早く止めすぎると右エッジのトレースが
   線から離れる向きに操舵する。**「開始時が黒なら動かさない」分岐も、まだ一度も発動していない。**
3. **共有 `brake()` にタイムアウトも中断条件もない。**

   ```cpp
   void Robot::brake() {
       leftMotor.brake();
       rightMotor.brake();
       while(stableCount < Config::BRAKE_STABLE_COUNT) { ... }  // 抜ける条件が「止まること」だけ
   }
   ```

   **停止判定が成立しないと戻ってこない。**中央ボタンでも抜けられない。
   `turnByImu` と `driveStraightByImu` の末尾で毎回呼ばれる（`driveStraight` は `stop()` なので対象外）。
   **チームへの確認事項。**

### 次にやるなら

**優先度順：**

1. **何もしない。**上の3つはいずれも現状で走り切れている。
   全体26.8秒に対して、残る改善余地は**弧移動で数十ms、浮きの対策で不明**。
   **ライントレース区間（外回りだけで18秒）の方が桁違いに大きい。**
2. 浮きが実害を出しているなら、**まずピッチ角速度を記録して、目視と対応が取れるかを確かめる。**
   対策はその後。
3. 共有 `brake()` の件は**チームに投げる。**デリバリー側は既に回避済みなので、こちらの実害はない。
