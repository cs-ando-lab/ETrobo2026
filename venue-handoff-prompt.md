# 試走会・本番環境への引き継ぎプロンプト（現時点の完成形）

更新日：2026-09-21。以下を会場のノートPC側のエージェントへ渡してください。
ユーザーが現時点の構成を一旦の完成形として採用しました。会場で必要な微調整は行えますが、
過去の最適化実験を勝手に再開しないでください。
「構成の採用」と「全色・全条件での実機検証済み」は別です。未確認事項は下記に残します。

## 最重要：現在採用している構成

- Deliveryの走行は `641c68f` 相当へ復元した構成を土台とする。
- 早期エッジ切り替えは撤回済み。青1通過を待ち、旧弧移動と低速接続を経て高速へ進む。
  青1の接続は通常reset、成功時も一度停止する旧方式。早期処理や止めない接続を復活させない。
- コーナーPIDも旧方式。抑制中の完了確認を分離する改善は今回の完成形には含まれない。
- Deliveryの戻り値が正常帰還完了の場合だけO/Dモードからラリーへ進む。
  正常完了を立てるのは **帰りの90度コーナー完了後900mmを走り終えた箇所だけ**。
  青検知・配置完了・中断・失敗・早期returnでラリーへ渡してはいけない。
- 初回の低速接近は、超音波110mm以下の有効値、または接近開始から**190mm**の早い方で終了。
  停止・整定後に初回アーム上げへ進む。190mmは調整用の距離上限で、競技中断条件ではない。
- 配置後の後退指令は **−90mm**。速度・duty上限60は維持。
- 正常色の通知はアーム下降後の既存の固定直進（左右35/40、1.0秒設定）へ重ねる。
  黄1・青2・赤3回、非ブロッキング。通知のために走行区間を延長しない。
- 190mmと−90mmは現行作業ツリーの値。コミットだけなら200mmと−80mmなので注意。

## あなたへの依頼

ETロボコン2026のDeliveryTaskを担当してください。
私はロボットを動かして挙動を観察し、ログを取得します。
あなたは基準ログと会場ログを比較し、原因を説明して、必要最小限の調整を提案してください。
レビュー依頼だけなら変更しないでください。実装を頼んだ場合に限って、合意した範囲を変更してください。

手元のコースは実際のコースを印刷して使ったものです。
本番・試走会では寸法、継ぎ目、表面、色の見え方などが多少違う可能性があります。
ただし違いがあること自体は未確認です。目的は、手元で最適化済みの走行を
現地でも安定して再現することであり、会場で一から速度探索をやり直すことではありません。
コースアウト・誤配置を避けることを最優先に、基準との差を調べて微修正してください。

## 1. 最初に確認すること

1. リポジトリの場所と適用されるAGENTS.md、CLAUDE.mdを確認する。
2. `git status --short`、ブランチ、HEAD、基準からの差分を確認する。
   私やチームメンバーが変更したファイルを勝手に戻さない。
3. 下記文書を読む。ただし履歴中の「最新」「採用」「実機未確認」は当時の記述であり、
   最新コード・確定版の基準記録と照合する。古い定数やモード説明を復活させない。
   - `curve-section-review.md`：低速トレース後から青1本目までの曲線。
   - `straight-speed-review.md`：直線・コーナー後の最終青への接近。
   - `delivery-arm-and-area-review.md`：アーム・配置・復帰の経緯。関連する場合のみ。
4. 起動モードと実際の呼び出し先をGameRunnerで読み取り確認する。
   過去のT/B/D表記から推測しない。T内の条件Bと起動モードBも別物。
5. 基準コードと会場コードが同じか、ログがどの版・色・コース・モードの走行かを確認する。
   不明なら「比較条件不明」と報告し、差をコースのせいだと断定しない。

CLAUDE.mdにも古い説明が残る場合があります。例えば「自動テストは存在しない」と
「tests/run.shによるホスト試験」が併記されているため、実在するファイルと実装を確認してください。

## 2. 変更範囲と開発ルール

- 主対象は`main/app/tasks/DeliveryTask.cpp`、`.h`およびDelivery専用部品。
- RallyTask、SumoTask、GameRunner、Calibrator、他メンバーの機能は変更しない。
  接続の問題ならまず読み取り確認と報告。必要な変更は私に説明してから行う。
- `Robot`、`Tracer`、`Pid`、`ColorJudge`、`Config`、共通ログは共有物。
  原則としてDeliveryローカルで解決する。共有変更が必要なら利用箇所と影響を説明し、
  明示的に使った経路だけ変わる方法を選ぶ。共通既定値を一括調整しない。
- test.cppも勝手に書き換えない。私が試験実装を依頼した場合だけ対象とする。
- アームの狙いは機械的な上端ではなく、上端より手前の決まった姿勢。
  色が読めたことだけで十分上がったと判断しない。上端へ押し付けたりトルクを無断で上げない。
- ボトル捕捉用の低速区間の長さ、配置後の後退などは私の目視と相談して調整する。
  ログ時間が長いという理由だけで削らない。接近距離の代替条件は今回採用済み。
  調整箇所はDeliveryTask.cpp上部の `kApproachFallbackDistanceMm` と `kAreaBackwardMm`。
- 往復の90度コーナーは左右が違う同じコーナー。
  振り戻し後の正味回頭角が小さいことから、帰りのコーナー自体が浅いとは推定しない。
- 新しい自動中断条件を勝手に追加せず、削除・緩和済みの条件も復活させない。
  アーム未達、色再試行の前進不足、配置転回後の残留速度は現状の続行方針を維持する。
  既存上限を無断削除して無制限走行にもしない。未検知時に距離だけで配置を開始しない。
  Deliveryが失敗終了した後にラリーへ進めない。ボタン中断APIの再設計は現在の依頼範囲外。
- 計測用と制御用の変数・カウンタを分ける。計測追加で制御状態を更新しない。
- 1回の比較で原則1要因だけ変え、理由・変更値・戻し方・実機確認の有無を残す。
- 未コミット変更を保存したまま作業し、全体整形・不要なリファクタ・破壊的Git操作をしない。
  コミット・push・PR・マージ・実機転送は私が依頼した場合のみ。
  Git運用は既存のチーム規約に従い、mainへ直接pushしない。
- ビルド成功・ホスト試験成功と実機の走行成功は別。未検証を成功扱いしない。

## 3. 採用版・基準データ

| 項目 | 内容 |
|---|---|
| 採用時のHEAD | `918e61e`。641c68f相当の復元＋帰還結果判定＋後退微増＋非ブロッキング通知＋接近距離代替 |
| 必須の未コミット差分 | DeliveryTask.cppの接近上限200→190mm、後退−80→−90mm。コミットと別に必ず携行する |
| その他の未コミット差分 | GameRunner.cpp、RallyTask.cpp、test.cpp、test.h。本人・各担当と必要範囲を確認し、勝手に捨てない |
| 完成形の基準ログ | 190mm／−90mm／通知統合後の対応ログは本更新で未指定。後述の旧ログを完成形検証済みと扱わない |
| 基準設定一覧 | 本書の値は更新時の作業ツリーから確認。持込時に再照合 |
| コースL/R、車体・タイヤ・センサー配置 | 確定時に記録 |
| 起動モード・初期位置・ボトル配置・アーム初期位置 | 確定時に記録 |
| バッテリー状態、用紙/床、照明、走行時の目視 | 基準走行と一緒に記録 |
| 計測仕様・ログ欠落・既知の制約 | 確定時に記録 |

`92e0de1`が復元とラリー移行制限、`4c315d5`が後退−80mm、`d13d328`が通知統合、
`918e61e`が接近200mm代替。現在値190mm／−90mmはその後の未コミット調整。
後続のコミット名だけで適用値を判断しない。持込時のブランチ・HEAD・差分を保存する。

参考ログ（いずれも旧条件）：

- `20260921_180341.csv` / `180503.csv`：赤。正常帰還したが行きコーナー後の黒連続が残った。
- `pybricks-data-2026-09-21T09-30-54.csv`：赤、正常帰還結果1を確認した復元後の走行。
- `20260921_184329.csv`：赤、後退−75mm指令、Delivery aborted=0。
- `20260921_184553.csv`：赤、後退−75mm指令。配置後接続で100mm／704ms／＋30.2度、
  反射率43→99、heading limitにより失敗。後退増量のきっかけだが原因確定ではない。

最後の2ログは190mm／−90mmの検証ログではない。会場へ行く前に可能なら現在値で基準を採る。

基準は一度の最速記録ではなく、同じ条件の複数回を残してください。
可能なら黄・青・赤をそれぞれ反復し、中央値と範囲、失敗例も保存します。
色間の総時間はルート長が違うので直接速度比較しません。

## 4. 距離・角度依存の確認表（採用時の現在値）

距離は車輪エンコーダー換算値です。地図上の直線距離ではありません。
開始姿勢・蛇行・滑り・タイヤ径・荷重・検出の遅れで変化します。

| 対象 | 原点・現在の条件 | 現地で比較すること |
|---|---|---|
| 初回ボトル接近 | Delivery冒頭でPWM50を設定した直後の車輪距離が原点。超音波有効値110mm以下、または前進190mm | `[Approach]`終了理由、実移動、超音波値、時間、実際のボトルとの位置 |
| 最初の曲線 | 低速トレース終了後の曲線開始。コース符号補正後−55度、または500mmで切替 | 成立した条件、切替位置/方位、後半の追従、青1までの距離 |
| 青1→行きコーナー | 持ち替え後の低速接続開始前が原点。通常950mm、踏み越え回復後800mmで減速 | 原点の姿勢、実減速位置、白連続開始、コーナー検知までの余裕 |
| 行きコーナー→配置の青 | コーナー完了位置が原点。色別距離窓内の実際の青で配置 | 途中青と目的青の分離、実減速/受付/検知距離、速度 |
| 配置後→帰りコーナー | 帰りの低速接続開始前が原点 | 配置・線探索終了姿勢、減速と検知開始の位置 |
| 配置後の後退 | 指令−90mm、duty上限60。惰性分は別計測 | 指令/実動/惰性/合計、ボトルとの離脱余裕、転回後接続の結果 |
| 帰りコーナー→終了 | 帰りコーナー完了から900mmでDelivery終了 | 実停止位置・向き、次課題への引き渡し位置。別担当の後続コードは変更しない |

最初の曲線は現在、前半PWM93/Kp0.38→後半78/Kp0.55。
Ki0.01/Kd0.02で、青1本目上でも78を維持する。
かつて後半78対80は各2回で約950mm時刻が平均4433.5/4429.5ms、優位差なし。
78の方が白側偏りが小さかった。Ki増加や全区間Kp増加は過去に問題があったため、
現地で根拠なく繰り返さない。詳しい条件・例外はcurve-section-review.mdを参照する。
同文書の500〜700mm等は当時の計測原点による解析区間で、コース上の固定座標ではない。

行きコーナー完了後の現在の配置計画（mm）：

| 色 | 予測最終青 | 減速開始 | 探索窓開始 | 上限 |
|---|---:|---:|---:|---:|
| 黄 | 450 | 100 | 330 | 650 |
| 青 | 735 | 485 | 615 | 935 |
| 赤 | 1005 | 755 | 885 | 1205 |

高速97→接近65。探索窓内で非青3連続を確認してから青5連続で成立。
窓開始時に既に青なら、一度青が切れるまで配置しない。
数値は「物理的な青の先端」ではなく、連続一致成立位置などの実測に依存する。
黄色は予測350mm手前で減速する設定で、他色の250mm手前と異なる。
黄100mmは採用設定。ただし今回の完成形の色別反復検証は未登録。

帰りの減速は黄270/青575/赤880mm、コーナー検知受付開始は黄400/青705/赤1010mm。
色ごとの305mm差が会場でも妥当か確認する。往路の予測間隔へ機械的に揃えない。
現行は641c68f相当なので、帰りの青はコーナー判定抑制に使用するが、帰還終了条件には使わない。

### 既知の制約

- コーナー後の黒側への入り込みは、過去ログで残っている。全撤回で解消済みとはしない。
- コーナー再捕捉後の完了確認は抑制中に実行されない旧方式。既知事項として扱い、無断改修しない。
- 正常帰還フラグは途中終了からのラリー誤起動を防ぐが、コーナー自体の誤認を保証して防ぐものではない。
- 詳細ログ出力と欠落対策も旧方式へ戻っている部分がある。以前の`finalDiag`やキュー保護が
  現行にもあるとは扱わない。`outboundDiag`は出力呼出時間であり、PC受信完了までの時間ではない。
- アームのfalseにはボタンと未達が混在する。ユーザーは現段階でこのAPI改修を求めていない。
- 接近距離はエンコーダ換算。190mmで適切に読めることや、空転時の実移動を保証しない。

## 5. 基準取得前に揃えたい計測（追加実装は別途依頼する）

既存ログをまず調べ、足りない項目だけ追加を提案してください。
この文書はログ追加を実装済みとするものではありません。
最終基準ログを取得する前に計測仕様を揃え、会場でも同じ仕様で採ります。

### 既存ログで利用するもの

- DTrace：区間時間、入口150mm/以降の誤差・操舵、白黒連続、出力頭打ち、相対方位。
- BlueRun/BlueGate/BluePosition：生の青候補の位置・連続数・受付状態・車輪速度。
- AreaBlue：予測/実減速/探索/候補/最終青の位置、減速後の移動。
- Handoff、コーナー検知、Pivot stats：接続結果、旋回の結果、線の検出状況。
- アーム・配置・帰還とDelivery総時間。診断出力時間は走行時間と分ける。
- `[Approach]`：超音波／距離の終了理由、調整上限、実移動、超音波値、経過時間。
- `[Notify]`：実際の通知回数、色確定→下降開始時間、固定移動の最大制御間隔。
- `[Delivery] returnCompleted`：正常帰還距離終了であること。中断後のラリー移行を防ぐ結果。

存在・単位・原点・出力条件をコードで確認してから使ってください。
曲線単体テストで記録していた25mm刻み等が、通しDeliveryにもあるとは限りません。

### 追加候補と必要性

1. 走行識別：ビルド版/設定識別子、色、L/R、走行番号、区間原点。
   自宅/会場、置き方、電圧、目視結果は付随メモでもよい。
2. 曲線切替：曲線開始からの時間・距離・方位、角度条件/距離条件/同時成立、
   切替直前後の設定。最初に成立した条件を区別する。
3. 曲線の形：25mm等の間隔で時刻・相対方位・実制御反射率・操舵・速度を固定領域へ記録。
   周期出力ではなく停止後に出す。既存集計で十分なら重複実装しない。
4. 距離イベント：全ての減速/受付/停止について、原点名、予定距離、実距離・時刻・方位。
   青や白の「最初の候補」と「連続成立」の両方を記録し、検知遅れを分ける。
5. 距離ゲート前の観測：高速中/受付前も青候補と白連続を観測する。
   特に帰りの受付前にコーナーらしい白連続が既に始まっていないかを見る。
   ただし観測結果を制御へ使わず、物理的なコーナーと断定もしない。
6. 停止の実体：終了指令時と整定後の移動差は、既存の停止時間内で取得できる場合に記録。
   計測のために後続への引き渡しを遅らせる場合は別途相談する。

計測は同じ制御サンプルを再利用し、診断のための追加センサー読みやブロッキングビープを避ける。
固定容量、欠落数、走行ごとの初期化を設ける。大きな配列をタスクスタックへ置かない。
syslogの引数数制約を守り、制御周期の最大間隔も比較する。
「ログだから無影響」とは扱わず、計測追加前後で挙動が変わらないか確認する。

## 6. 自宅と会場の比較手順

1. まず同じ版・同じ色・同じL/R・同じ開始方法で走る。変更前の会場ログを保存する。
2. ログ全体を走行番号ごとに分ける。中断・再試行・失敗を成功走行に混ぜない。
3. 区間原点を合わせ、曲線開始→切替→青1→持ち替え→コーナー検知/完了→
   最終青→配置/帰り接続→帰りコーナー→終了の順に差を出す。
4. 内部時計で時間差を計算する。BLEのPC受信時刻は遅延・まとめ受信がある。
   生COLORが約1Hzしか残らないログでは、サンプルがないことを「青を跨いでいない」証拠にしない。
5. 車輪カウントのリセットを跨いで差を取らない。左右コースの方位符号を合わせる。
6. 複数回の中央値・最小最大・失敗率と、目視した停止位置/姿勢を比較する。
   車輪距離だけから「本番コースが何mm伸びた」とは結論しない。

原因の切り分け例：

| 見えた差 | まず疑う/確認すること | 避けること |
|---|---|---|
| 1区間以後のイベント位置がまとめてずれる | 原点の物理位置、入口姿勢、先行動作の惰性 | 全距離へ同じ補正を一括適用 |
| 複数区間で距離比が変わる | 印刷倍率、タイヤ/換算、滑り。可能ならコースを実測 | エンコーダーだけで倍率変更 |
| 青候補位置は似るが確定が遅れる | 速度、HSV/反射率、連続数、受付ゲート | 距離を先にずらす/彩度閾値を即緩和 |
| 曲線切替条件が角度から距離へ変わる | 入口方位、IMU、走行軌跡、曲線形状 | PWM/PID/切替位置を一度に変更 |
| コーナー後だけ最終青がずれる | ピボットと接続姿勢、完了位置、追従軌跡 | 振り戻し角からコースの曲がり角を断定 |

反射率の符号つき平均だけでは振れが相殺されます。平均絶対誤差・白黒連続・操舵も見ます。
コーナーのMAX_TURNは角度上限、TIMEOUTは時間上限。混同しないでください。

## 7. 現地調整の順序・採否

1. 初期位置、車体、電圧、センサーとコースの見え方を揃える。
2. どのイベントから差が出たかを特定し、距離窓/減速開始等の局所的な変更を提案する。
3. 最終青では直前の別の青との分離を確認する。予測値を変更すると減速・探索・上限も
   派生して動くので、全ての計算結果を表にしてから変更する。
4. 受付が遅れて対象を通過しても、安全上限だけ延ばして走り続けさせない。
   受付前の観測から原因を確認し、未検知なら停止する契約を維持する。
5. 曲線は切替位置/条件と入口姿勢を先に確認し、必要な場合だけ局所PWM等を検討する。
   基準の500〜700mmに無理に合わせず、方位・色イベントも対応付ける。
6. 変更前後を同条件で反復し、全体成功・配置・帰還まで確認する。
   速くなっても回復や安全停止が増えたら採用しない。

報告は「確認できた事実／原因の仮説／追加確認／変更案／実機結果」を分け、
原点・単位付きで自宅値、会場値、差、調整候補を提示してください。
コード変更時は関連レビュー文書へ理由と結果を追記します。

## 8. ノートPCへ持っていくもの

- 確定版コード一式、コミットID、必要な未コミット差分、設定一覧。
- 本書と上記レビュー文書。作成時点ではレビュー文書がgit未追跡なので、
  clone/pullだけでは移りません。意図的にコピーするか、許可を得て文書をコミットします。
- 自宅基準CSV原本、走行メモ、色/条件対応表。CSVもgit管理外の可能性があります。
- SDK・ビルド/転送環境、BLE Monitor、USB/BLEの動作確認。
  出発前にノートPCでビルド・接続・ログ保存まで試しておきます。

旧PCのWindowsダウンロード先は`/mnt/c/Users/cheba/Downloads`でしたが、
ノートPCで同じパスと決めつけないでください。最初に保存先を確認してください。
「直近N件」と言ったら対象CSVを一覧にしてファイル名・日時を示し、そのN件を使います。
更新時刻だけで基準CSVやコピー済みファイルを混ぜず、曖昧なら確認します。
1ファイルに複数走行が含まれる場合もあります。

## 持込前の更新チェック

- 基準コミット/差分、設定表、原点、起動モードを現在のコードで再確認。
- 基準CSVを登録し、各色の区間時間・距離・ばらつき・目視結果を追記。
- 計測の追加済み/未実装、取得できない項目、既知の制約を明示。
- 暫定値・古い操作説明を整理し、ノートPCのファイルと一致することを確認。
- 現在進行中の最適化履歴ではなく、最終的に採用した動作と安全条件だけを引き継ぐ。
