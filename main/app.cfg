INCLUDE("tecsgen.cfg");

#include "app.h"

CRE_TSK( MAIN_TASK, {
     TA_ACT,
     0,
     main_task,
     MAIN_PRIORITY,
     STACK_SIZE,
     NULL
} );

/* センサー値のBLE Monitor送信を、ライントレース中か各課題実行中かに関わらず
 * 電源投入後は常に一定周期で行うための独立タスク（main_taskの処理内容に依存しない） */
CRE_TSK( DEBUG_LOG_TASK, {
     TA_NULL,
     0,
     debug_log_task,
     DEBUG_LOG_PRIORITY,
     STACK_SIZE,
     NULL
} );
CRE_CYC( DEBUG_LOG_CYC, {
     TA_STA,
     { TNFY_ACTTSK, DEBUG_LOG_TASK },
     DEBUG_LOG_PERIOD,
     0
} );
